// operadore ternario


//variavel booleana 
bool ativo = true;

// variavel status recebendo a variavel booleana e verificando atraves de operadores relacionais a condicao de imprecao textual
string status = ativo == true ? "Cadstro ativo": "Cadstro inativo";

Console.WriteLine(status);
